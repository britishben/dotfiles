### install to /etc/bash_completion.d ###

_tma() {
    TMUX_SESSIONS=$(tmux list-sessions 2>/dev/null | cut -d ':' -f 1 | xargs)

    local cur=${COMP_WORDS[COMP_CWORD]}
    COMPREPLY=( $(compgen -W "$TMUX_SESSIONS" -- $cur) )
}
complete -F _tma tma
